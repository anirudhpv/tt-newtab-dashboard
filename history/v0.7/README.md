# v0.7 — Open-Meteo + Design Refresh
Dropped OpenWeatherMap (no API key). Open-Meteo + Nominatim for weather.
Fraunces font, noise texture background, refined spacing.
To be reconstructed by Claude Code per CLAUDE.md instructions.
